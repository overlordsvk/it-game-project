<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Hra</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <div class="navbar-header">
                @Html.ActionLink("Hra", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">@Html.ActionLink("Postava", "Index", "Character", new { @class = "nav-link" })</li>
                        <li class="nav-item">@Html.ActionLink("Skupina", "Index", "Group", new { @class = "nav-link" })</li>
                        <li class="nav-item">@Html.ActionLink("Správy", "MailBox", "Messaging", new { @class = "nav-link" })</li>
                        <li class="nav-item">@Html.ActionLink("Súboj", "List", "Fight", new { @class = "nav-link" })</li>
                        <li class="nav-item">@Html.ActionLink("Inventár", "Index", "Inventory", new { @class = "nav-link" })</li>
                    }
                    <li class="nav-item">@Html.ActionLink("Skupiny", "List", "Group", new { @class = "nav-link" })</li>
                    <li class="nav-item">@Html.ActionLink("Zoznam Postáv", "List", "Character", new { @class = "nav-link" })</liclass="public">
                </ul>
                <ul class="navbar-nav pull-right">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item"> <a class="nav-link" href="~/Account"> @Html.Action("GetUserName", "Account", null)</a></li>
                    }
                    @if (!User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">@Html.ActionLink("Prihlásiť sa", "Login", "Account", new { @class = "nav-link" })</li>
                        <li class="nav-item">@Html.ActionLink("Registrácia", "Register", "Account", new { @class = "nav-link" })</li>
                    }
                    else
                    {
                        <li class="nav-item">@Html.ActionLink("Odhlásenie", "Logout", "Account", new { @class = "nav-link" })</li>
                    }
                </ul>
            </div>
        </div>
    </nav>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Game</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
